# v23 handoff — simplified hand/wrist collision + fixed diagnostic camera

Written 2026-07-17. Paste this into a fresh interactive session to pick up
where a frozen terminal left off. Everything below was verified against the
actual files on disk just now (not just recalled from conversation) —
diffed the XML, grepped the wiring code, and loaded the model in MuJoCo to
confirm it compiles.

## TL;DR

**v23 appears fully implemented and wired up already**, not just started.
The remaining work is: smoke-test a short rollout, confirm the
broadphase-overflow warnings are actually gone, then launch a real training
run. See "Next steps" at the bottom.

## Status of the other terminal's v22x job (unrelated to v23, FYI)

While investigating, I checked on the `--no-joystick` (v22x, arm+hand
reach-tracking only, no joystick) run that was reportedly stuck:

- PID 1274638, `train_mouse_janelia_arm_hand.py --no-joystick --tag
  v22x-arm-only-run2-multigpu --eval-every 5000000`, started 02:50:18,
  **still alive**, both A40 GPUs at 99% utilization (i.e. actively
  computing, not deadlocked/hung on I/O).
- Log (`logs/janelia_v22x_arm_only_run2.log`) shows: wandb init OK, first
  eval video rendered (the imageio "resizing to macro_block_size" line is
  benign), then an `os.fork() was called... incompatible with
  multithreaded code` RuntimeWarning from a subprocess call — this is a
  known-benign JAX+subprocess warning, not a crash. No traceback.
- Most likely explanation: first-step XLA compilation across 2 GPUs
  (`pmap`) is simply slower than v21's single-device runs, and nothing has
  printed to the log yet because the next log point is presumably at the
  next eval/logging interval. GPU util at 99% is the reassuring signal —
  if it were actually deadlocked from the fork() warning, GPU usage would
  be flat at 0%.
- **Action: leave it running.** No evidence of a hang; just slower
  multi-GPU warm-up than you're used to from v21.

## What's already done for v23

All of this was found already present in the repo (`git status` shows
these as staged/untracked changes from the frozen session, not something I
just wrote):

### 1. New XML: `vnl_playground/tasks/mouse/xmls/mouse_forelimb_right_janelia_arm_hand_v23_simplified_collision.xml`

Diffed against `mouse_forelimb_right_janelia_arm_hand_v22_contacts_patched.xml`
(the v22 baseline). Three changes:

- **All 38 wrist/hand bones that previously collided as raw meshes**
  (7 carpals, 5 metacarpals, 14 phalanges, hand sesamoids — everything
  from `joint_stiffness`-tracked comments as "all 7 wrist carpals, all 5
  metacarpals, all 14 phalanges, and all hand sesamoids") **now collide via
  a simplified ellipsoid primitive instead of the mesh itself.** Pattern
  per bone:
  ```xml
  <geom name="Foo_bone" ... class="hand_visual" contype="0" conaffinity="0"/>   <!-- visual only now -->
  <geom name="Foo_bone_col" type="ellipsoid" size="..." pos="..." quat="..." contype="2" conaffinity="1" group="1"/>  <!-- new collision proxy -->
  ```
  Sizes/positions/quats for every `_col` geom were copied directly from the
  reference full-body rig you pasted earlier in this conversation (same
  bone names — `Metacarpal_hand_1_right_bone`, `Phalanx_hand_2_3_right_bone`,
  etc. — so the numbers transfer 1:1). I checked: **all 38 bones that had
  collision enabled in v22 have a matching `_col` proxy in v23, no gaps.**
  Rationale (from the file's own header comment): mesh-vs-primitive contact
  generates far more broadphase candidate pairs per touching pair than
  primitive-vs-primitive, which was the likely actual driver of the
  490k+ "broadphase overflow" warnings from v22 training — not *which*
  bones had collision enabled (that was already scoped down to just
  wrist+hand on 2026-07-16).
- **Joystick reverted to the raw stac-mjx-synced pos/quat**, undoing an
  earlier hand-patch that manually zeroed the shaft tilt and translated
  `joystick_base` to force exact fingertip contact. Per Eric, that manual
  fix was "the big mistake all along" — v23's bet is that the simplified
  collision primitives (more forgiving contact resolution) make real
  contact work despite the joystick's genuine ~38.6° shaft tilt, rather
  than fixing the geometry itself.
- **The fixed diagnostic camera added** (`arm_joystick_fixed_view`) — same
  camera described in the "Fixed camera angle" doc: rolled so the joystick
  shaft renders vertical regardless of its real tilt, frames both
  shoulder/arm and joystick. Not load-bearing for physics/training, just
  for legible renders.

I additionally just re-verified the file **loads and compiles cleanly** in
MuJoCo (`mujoco.MjModel.from_xml_path` + one `mj_forward`): `nq=27,
ngeom=86`, 8 contacts active at the rest pose (qpos=0) — not yet checked
whether those 8 are expected (hand self-contact / near-joystick-ball
proximity at rest) or a red flag; worth a look before a long training run,
but not a compile error.

### 2. Config wiring: `vnl_playground/tasks/mouse/consts.py`

`JANELIA_MOUSE_ARM_HAND_V23_XML_PATH` (line 76) already points at the new
XML, with a comment matching the rationale above almost verbatim — the
frozen session had already written up why before it froze.

### 3. Task config: `vnl_playground/tasks/mouse/imitation_arm_hand.py`

`default_config_v23()` (line 179) already exists, sets
`cfg.walker_xml_path = JANELIA_MOUSE_ARM_HAND_V23_XML_PATH`, reuses v22's
reference data (same kinematic tree, joystick unaffected by the collision
change).

### 4. CLI wiring: `vnl_playground/train_mouse_janelia_arm_hand.py`

`--v23` flag already added (line 88), mutually exclusive with
`--no-joystick` (line 130, asserted), wired to `default_config_v23()`
(line 134), and `env_name = "janelia-v23-simplified-collision"` (line 215)
for wandb naming.

## Trials to use for v23 (Eric's pick — only these 6, not the full v22 set)

Eric only wants these 6 trials for v23, identified by these reprojection
videos he reviewed:

- `/root/vast/eric/stac-mjx/v22_all_trials_3d_view/CFL_35_20240128_trial_0101_3d_fixedcam.mp4`
- `/root/vast/eric/stac-mjx/v22_all_trials_3d_view/CFL_35_20240128_trial_0201_3d_fixedcam.mp4`
- `/root/vast/eric/stac-mjx/v22_all_trials_3d_view/CFL_35_20240128_trial_0301_3d_fixedcam.mp4`
- `/root/vast/eric/stac-mjx/v22_all_trials_3d_view/CFL_35_20240128_trial_0401_3d_fixedcam.mp4`
- `/root/vast/eric/stac-mjx/v22_all_trials_3d_view/CFL_37_20240423_trial_0301_3d_fixedcam.mp4`
- `/root/vast/eric/stac-mjx/v22_all_trials_3d_view/CFL_37_20240423_trial_0401_3d_fixedcam.mp4`

I checked: all 6 exist on disk, and all 6 have matching reference clips
under `MOUSE_REFERENCE_DATA_JANELIA_V22_PATH`
(`/root/vast/eric/stac-mjx/refined_STACed_data_v22/<trial>/<trial>_ik.h5`),
which is what `default_config_v23()` already points `reference_data_path`
at. **But that directory has 15 trials total** (all of CFL_35, CFL_36, and
CFL_37), not just these 6 — so as-is, v23 training would use all 15, not
Eric's preferred subset.

**Action needed (not yet done anywhere in the repo):** restrict training to
just these 6. `MouseReferenceClips` (in
`vnl_playground/tasks/mouse/reference_clips.py`) already supports this via
a `keep_clips_idx` config field (`imitation.py:40`, default `None` = keep
all), which indexes into the `sorted(glob(**/*_ik.h5))` list over the data
directory. I computed the actual indices against the real directory
contents just now (not hand-counted — ran the same glob+sort the loader
uses):

```
0  CFL_35_20240128_trial_0001
1  CFL_35_20240128_trial_0101   <- keep
2  CFL_35_20240128_trial_0201   <- keep
3  CFL_35_20240128_trial_0301   <- keep
4  CFL_35_20240128_trial_0401   <- keep
5  CFL_36_20231220_trial_0001
6  CFL_36_20231220_trial_0101
7  CFL_36_20231220_trial_0201
8  CFL_36_20231220_trial_0301
9  CFL_36_20231220_trial_0401
10 CFL_37_20240423_trial_0001
11 CFL_37_20240423_trial_0101
12 CFL_37_20240423_trial_0201
13 CFL_37_20240423_trial_0301   <- keep
14 CFL_37_20240423_trial_0401   <- keep
```

So: `keep_clips_idx = [1, 2, 3, 4, 13, 14]`. Set this in
`default_config_v23()` (`imitation_arm_hand.py:179`), e.g.
`cfg.keep_clips_idx = [1, 2, 3, 4, 13, 14]`. **Caveat:** this list is only
valid as long as the directory's file contents don't change (add/remove a
trial and the sort shifts) — if `CFL_36`/other trials get added or removed
before this lands, re-run the glob+sort rather than trusting these numbers
blind. Safer long-term fix would be a name-based filter instead of
index-based, but that's a bigger change than this handoff should scope in.

## Next steps

1. **Wire up the 6-trial filter above** (`keep_clips_idx` in
   `default_config_v23()`) before running anything real — otherwise v23
   silently trains on all 15 trials instead of Eric's chosen 6.
2. **Smoke-test before committing GPUs to a long run**: a short rollout
   (a few hundred steps, small `--num-envs` if there's a flag for it, or
   just watch the first eval render) to confirm:
   - No broadphase-overflow warnings in the log (the whole point of v23).
   - The hand/joystick actually make contact under the new ellipsoid
     proxies — render a video using `arm_joystick_fixed_view` and eyeball
     it, or check `ncon`/penetration depth isn't wildly different from
     v22's mesh-based contact.
   - Nothing about the 8 resting contacts I found is pathological (e.g.
     self-penetrating fingers at qpos=0 that'll blow up on the first
     integration step).
3. **If the smoke test looks right, launch training**:
   ```
   python vnl_playground/train_mouse_janelia_arm_hand.py --v23 \
     --tag <your-tag> --eval-every <something-visible, e.g. 5000000>
   ```
   (mirroring the `--eval-every 5000000` override already used for the
   v22x run, since the script's default of 100M is described in its own
   `--help` text as "too sparse to see progress on a multi-hour run").
4. **Compare against v22's mesh-collision run** (if one exists/is
   running) for contact quality and broadphase-warning counts, to confirm
   v23 is actually the fix and not just a different set of problems.

## Appendix: full "Fixed camera angle" doc (verbatim)

This is the complete doc Eric wants carried forward as-is into the v23
session — reproduced in full below rather than just summarized, since he
asked for it explicitly. It's already embedded in the v23 XML itself (see
"What's already done for v23" above) as the `arm_joystick_fixed_view`
camera element; this is the reasoning behind those numbers.

---

# Fixed camera angle for v22 arm+joystick visualization (shaft rendered vertical)

## TL;DR

For any diagnostic/visualization rendering of the v22 arm+hand+joystick model
in MuJoCo (not the calibration-matched reprojection-onto-video renders,
which use their own per-trial camera derived from each trial's real
calibration — see `stac_mjx/reproject_stac_model.py`), use the fixed camera
below. It's rolled so the joystick shaft renders **vertical in the image**,
regardless of the shaft's actual ~38.6° tilt in the model's own coordinate
frame (see `2026-07-16-v22-registration-fix-and-joystick-anchor-update.md`
for why that tilt exists and why it's currently unresolved/disputed — this
doc is unrelated to that argument, it's purely about picking a legible,
reproducible viewing angle for any future renders, independent of how that
tilt question gets resolved). It also frames both the shoulder/arm and the
joystick in the same shot.

## Camera parameters

As a MuJoCo `<camera>` element (add to `<worldbody>`, or construct via
`mujoco.MjvCamera`/xyaxes at render time):

```xml
<camera name="arm_joystick_fixed_view"
        pos="-0.03940696 -0.04889572 0.09061815"
        xyaxes="0.65772284 -0.75326002 0.00000000  0.46960983 0.41004846 0.78187395"
        fovy="45"/>
```

- `pos`: camera position in world/model frame (meters).
- `xyaxes`: first 3 numbers = camera "right" axis, last 3 = camera "up" axis
  (MuJoCo xyaxes convention — forward/view direction is `-cross(right, up)`).
- `fovy`: 45 degrees, arbitrary/adjustable to taste — not load-bearing for
  the "shaft vertical" property, only affects zoom.

This was derived and verified against
`stac-mjx/refined_STACed_data_v22/CFL_35_20240128_trial_0001/CFL_35_20240128_trial_0001_ik.h5`,
rendered directly against `models/mouse_forelimb_right_janelia_arm_hand_v22_stac.xml`
(no video compositing — this is a clean, from-scratch MuJoCo render, so it
should transfer directly to any v22 model file with the same `joystick`
body orientation and same `humerus`/`joystick_base` body positions; if
either of those change, e.g. from re-deriving `joystick_base` upright per
the open item in the registration-fix doc, this camera should be
re-derived — see "How to reproduce/re-derive" below).

## How it was derived

1. **`up` axis = the joystick shaft's actual world-frame direction.** Read
   the `joystick` body's world orientation (`xquat`) via `mj_forward` at
   rest (qpos=0), and rotate its local `+Z` (the shaft's own long axis) into
   world frame: `shaft_dir = R(xquat) @ [0,0,1]`, normalized. Setting the
   camera's `up` to this vector is what makes the shaft render vertical —
   it doesn't matter whether that tilt is "real" or an artifact; the camera
   simply rolls to match whatever the model currently does.
2. **`right` axis**: `cross(shaft_dir, world_ref)`, normalized, where
   `world_ref = [0,0,1]` (or `[1,0,0]` as a fallback if `shaft_dir` is
   nearly parallel to `[0,0,1]`, to avoid a degenerate cross product — not
   triggered for this model's actual tilt, but kept for robustness if the
   tilt changes).
3. **`forward`** (the direction the camera looks into the scene):
   `-cross(right, up)`, normalized (MuJoCo xyaxes convention).
4. **`lookat`**: midpoint between the `humerus` body's world position
   (`[-0.0071983, -0.01418874, 0.07143588]` — this is the same point
   `register_v22_mocap.py` calls `targets["shoulder"]`/`model_shoulder`,
   just read off the `humerus` body directly here since it's the visible
   mesh body nearest that anchor) and the `joystick_base` body's world
   position (`[-0.01264354, -0.03211015, 0.04737563]`).
5. **`distance`**: `span * 1.15 + 0.015` meters, where `span` is the
   distance between those same two points (`~0.0305m` for this model) —
   tuned empirically so both the shoulder/arm mesh and the joystick fill
   most of the frame without clipping either. `pos = lookat - forward * distance`.

## How to reproduce/re-derive

If the model changes (new `joystick_base` position/orientation, e.g. from
the pending upright re-derivation), re-run the same steps in a MuJoCo
Python session — `mj_forward` at qpos=0 to get body world positions/orientations,
then steps 1–5 above. This is a ~30-line script; ask for it again if useful
rather than hand-deriving new numbers, since the reasoning depends on
whichever XML you're pointing at matching the actual body names used above
(`joystick`, `joystick_base`, `humerus`).
